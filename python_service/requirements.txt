numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
# Tensorflow ist speziell je nach CPU (das lassen wir so, das ist gut)
tensorflow-cpu; platform_machine != "aarch64" and platform_machine != "armv7l"
tensorflow; platform_machine == "aarch64"